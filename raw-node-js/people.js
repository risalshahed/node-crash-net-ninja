
const people = ['rick', 'nick', 'sick', 'kick'];
const age = [10, 20, 30, 40]
const type = ['male', 'male', 'physic', 'action']
// console.log(people);

// export multiple data
module.exports = {
  // property: value (alada name o hoite pare, same dewa MANDATORY NAA)
  // people: people,
  // age: age
  // shorthand if property & value is same
  people,
  age,
  type
}